#include<stdio.h>
int add(int,int);
int main()
{
	int a,b,c;
	printf("enter two numbers : ");
	scanf("%d%d",&a,&b);
	c=add(a,b);
}
int add(int a,int b)
{
	int add;
	add=(a+b);
	printf("addition : %d",add);
}
