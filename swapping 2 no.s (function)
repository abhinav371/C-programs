#include <stdio.h>
void swap(int x, int y)
{
	int temp=x;
	x=y;
	y=temp;
}
int main()
{
	int x, y;
	printf("Enter Value of x : ");
	scanf("%d",&x);
	printf("\n Enter Value of y :  ");
	scanf("%d",&y);
	swap(x,y);
	printf("\n After Swapping: x = %d, y = %d", y,x);
	return 0;
}
