#include<stdio.h>
int main()
{
	int a[10],n,i,k;
	printf("enter the size : ");
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
		scanf("%d",&a[i]);
	}
	printf("enter the value to be inserted : ");
	scanf("%d",&k);
	a[i]=k;
	for(i=0;i<=n;i++)
	{
		printf("\n %d",a[i]);
	}
}
