#include<stdio.h>
int max(int x,int y);
main()
{
	int c,x,y;
	printf ("enter the values : ");
	scanf("%d%d",&x,&y);
	c=max(x,y);
	printf("%d is maximum number",c);	
}
int max(int x,int y)
{
if(x>y)
return x;
else
return y;	
}
