#include<stdio.h>
int main()
{
	int i,a[10],sum=0,n,avg;
	printf("enter the size: ");
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
		printf("enter the elements : ");
		scanf("%d",&a[i]);
		sum=sum+a[i];
	}
	avg=(sum/n);
	printf("sum=%d",sum);
	printf("avg=%d",avg);
}
