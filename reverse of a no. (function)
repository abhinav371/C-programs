#include<stdio.h>
int Reverse(int n)
{
   int sum=0;
   while (n!=0)
   {
    sum=sum*10+n%10;
    n/=10;
   }
   return sum;
}
int main()
{
   int n,rev;
   printf("Enter a number: ");
   scanf("%d",&n);
   rev=Reverse(n);
   printf("The reverse of %d is: %d",n,rev);
   return 0;
}
